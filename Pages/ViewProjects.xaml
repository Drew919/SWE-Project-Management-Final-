<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArchonPM.Pages.ViewProjects"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchonPM.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Projects" FontSize="30" FontWeight="SemiBold"/>
            <Button Grid.Column="1" Content="Create Project" Click="CreateProject_Click"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,16,0,8">
            <TextBox
                x:Name="SearchBox"
                Header="Search"
                PlaceholderText="Name or owner"
                MinWidth="220"
                TextChanged="FilterChanged"/>
            <ComboBox
                x:Name="StatusFilterBox"
                Header="Status"
                MinWidth="160"
                SelectionChanged="FilterChanged">
                <ComboBoxItem Content="All statuses" Tag=""/>
                <ComboBoxItem Content="Planning" Tag="Planning"/>
                <ComboBoxItem Content="Not Started" Tag="Not Started"/>
                <ComboBoxItem Content="In Progress" Tag="In Progress"/>
                <ComboBoxItem Content="Completed" Tag="Completed"/>
            </ComboBox>
        </StackPanel>

        <StackPanel x:Name="EmptyStatePanel" Grid.Row="3" Spacing="12" Visibility="Collapsed">
            <TextBlock
                Text="No projects yet. Create your first project to get started."
                FontSize="16"
                Opacity="0.7"
                TextWrapping="WrapWholeWords"/>
            <Button Content="Create Project" Click="CreateProject_Click" HorizontalAlignment="Left"/>
        </StackPanel>

        <ListView
            x:Name="ProjectsList"
            Grid.Row="3"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="ProjectsList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ProjectListItem">
                    <Border
                        Padding="14"
                        Margin="0,0,0,10"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="4">
                        <StackPanel Spacing="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Text="{x:Bind Name}"
                                    FontSize="18"
                                    FontWeight="SemiBold"
                                    TextWrapping="WrapWholeWords"/>
                                <TextBlock
                                    Grid.Column="1"
                                    Text="{x:Bind Status}"
                                    FontWeight="SemiBold"
                                    Margin="12,0,0,0"/>
                            </Grid>
                            <TextBlock Text="{x:Bind OwnerLine}" FontSize="14" Opacity="0.8" TextWrapping="WrapWholeWords"/>
                            <TextBlock Text="{x:Bind DescriptionPreview}" FontSize="14" TextWrapping="WrapWholeWords"/>
                            <TextBlock Text="{x:Bind CountsLine}" FontSize="12" Opacity="0.75"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
