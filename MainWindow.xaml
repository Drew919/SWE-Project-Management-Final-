<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ArchonPM.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ArchonPM">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TitleBar x:Name="AppTitleBar"
                      Title="ArchonPM"/>

            <Border Grid.Row="1"
                    Padding="12,8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="0,0,0,1"
                    Background="{ThemeResource LayerFillColorDefaultBrush}">
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- ArchonPM Branding -->
                    <TextBlock Text="ArchonPM" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>

                    <Button x:Name="DashboardButton"
                            Grid.Column="1"
                            Content="Dashboard"
                            Click="DashboardButton_Click"
                            AutomationProperties.Name="Dashboard"/>

                    <ComboBox x:Name="ProjectSelectorBox"
                              Grid.Column="2"
                              MinWidth="180"
                              MaxWidth="300"
                              Padding="8,4"
                              SelectionChanged="ProjectSelectorBox_SelectionChanged"
                              AutomationProperties.Name="Project selector">
                        <ComboBox.Header>
                            <TextBlock Text="Project" FontSize="12" Opacity="0.7"/>
                        </ComboBox.Header>
                    </ComboBox>

                    <TextBlock x:Name="ContextLabel"
                               Grid.Column="3"
                               VerticalAlignment="Center"
                               TextWrapping="NoWrap"
                               TextTrimming="CharacterEllipsis"
                               Opacity="0.75"
                               AutomationProperties.LiveSetting="Polite"/>
                </Grid>
            </Border>

            <Frame x:Name="rootFrame" Grid.Row="2"/>
        </Grid>
    </Border>
</Window>
